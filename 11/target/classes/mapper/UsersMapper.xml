<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UsersMapper">

    <insert id="insertUser" parameterType="com.example.demo.entity.Users">
        INSERT INTO users (userId, userPwd, userName, userAge, userSex, userTel, createTime)
        VALUES (#{userId}, #{userPwd}, #{userName}, #{userAge}, #{userSex}, #{userTel}, #{createTime})
    </insert>

    <select id="findAllUsers" resultType="com.example.demo.entity.Users">
        SELECT * FROM users
    </select>
</mapper>